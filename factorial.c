#include<bits/stdc++.h>
using namespace std;
main()
{
    char s[100];
    int a[100],i;
    cout<<"Enter the number whose factorial is to be found";
    cin>>s;
    for(i=0;i<strlen(s);i++)
        a[i]=intb[i]-'0';
}
